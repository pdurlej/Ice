interface:
  display_name: "Program Fire"
  short_description: "Program the menu bar and Fireline with local agents"
  default_prompt: "Use Fire to inspect my menu bar and propose a safe Context Scene."

dependencies:
  tools:
    - type: "mcp"
      value: "fire"
      description: "Local Fire From Ice menu bar control plane"
      transport: "stdio"
